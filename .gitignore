node_modules/
.expo/
dist/
web-build/
*.tsbuildinfo
.DS_Store
# local-only: relaxes Cursor's agent command sandbox for this workspace
.cursor/sandbox.json
.env
.env.*
*.db
*.db-journal
.context/
app/ios/
app/android/
# local copy of the web export used to test production serving
server/public/
# uploaded images (dev)
server/uploads/

# playwright browser cache (wallet prototype, local only)
.pw-browsers/
